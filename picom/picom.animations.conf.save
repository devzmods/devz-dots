rules = (
{
	match = "window_type = 'normal'";
	animations = ({
		triggers = [ "close" ];
		scale-x = {
			curve = "cubic-bezier(0,1.3,1,1)";
			duration = 0.3;
			start = 1.0;
			end = 0.5;
		}
	})
}
)
